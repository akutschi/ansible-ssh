---
- hosts: localhost
  connection: local
  become: true
  pre_tasks:
    - name: Include test vars
      include_vars: "{{ playbook_dir }}/vars/main.yml"
  tasks:
    - name: Include role
      include_role: 
        name: ansible_ssh